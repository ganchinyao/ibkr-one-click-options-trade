import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {},
  chipContainer: {
    flexDirection: 'row',
    paddingHorizontal: 10,
  },
  chipStyle: {
    marginHorizontal: 5,
  },
});
